---
import Layout from "@/layouts/Layout.astro";

const { title } = Astro.props;
---

<Layout title={title}>
  <main class="empty-layout flex flex-col h-screen">
    <div class="flex flex-col grow">
      <slot />
    </div>
  </main>
</Layout>
